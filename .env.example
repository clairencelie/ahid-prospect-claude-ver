POSTGRES_USER=ahid
POSTGRES_PASSWORD=ahid
POSTGRES_DB=ahid_prospect
DATABASE_URL=postgres://ahid:ahid@db:5432/ahid_prospect?sslmode=disable

BLOCK_THRESHOLD=80
REVIEW_THRESHOLD=50
LOCK_TTL_DAYS=30

# Optional: AI enrichment (M7) uses Gemini with Google Search grounding when
# set. Leave blank to fall back to the static fixtures file (offline-safe).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

VITE_API_BASE=http://localhost:8080/api/v1
